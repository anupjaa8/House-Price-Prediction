# House-Price-Prediction
This machine learning regression pipeline automates data cleaning, handles right-skewed pricing with log-transformations, and converts categorical features into numerical formats. It then utilizes robust ensemble algorithms like Random Forests or XGBoost to accurately predict continuous property values, measuring performance via Root MSE
